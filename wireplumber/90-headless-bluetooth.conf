# Allow the dedicated lingering audio user to manage Bluetooth audio without
# an active local graphical seat/session.
wireplumber.profiles = {
  main = {
    monitor.bluez.seat-monitoring = disabled
  }
}
